# Environment Configuration
ENVIRONMENT=development          # Change to 'production' for production use

# Database Configuration
DATABASE_NAME=hoi-starter-db     # Replace with your database name (e.g., azure-todo-app)
DATABASE_URL=                    # MongoDB connection string (e.g., mongodb://username:password@host:port/database)

# Azure Key Vault Configuration
KEY_VAULT_NAME=your-key-vault-name          # Azure Key Vault name (e.g., msdocs-key-vault-123)
KEY_VAULT_SECRET_NAME_DATABASE_URL=database-url-secret # Secret name in Key Vault storing the DATABASE_URL

# Azure Credentials
AZURE_TENANT_ID=your-azure-tenant-id        # Azure Active Directory Tenant ID
AZURE_CLIENT_ID=your-azure-client-id        # Azure AD Application (Client) ID
AZURE_CLIENT_SECRET=your-azure-client-secret # Azure AD Client Secret for authentication
